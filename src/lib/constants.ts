/**
 * Central constants for UI components
 *
 * This file contains shared constants used across different UI components.
 * Use these constants to maintain consistency in the UI library.
 */
export const UI_CONSTANTS = {
  // Spacing and positioning
  COLLISION_PADDING: 16,
  SIDE_OFFSET: {
    DEFAULT: 4,
    TOOLTIP: 0,
  },

  // Timing
  DELAY_DURATION: {
    TOOLTIP: 0,
  },
}
