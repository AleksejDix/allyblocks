/**
 * Default duration (in ms) from when the mouse enters the tooltip trigger until the tooltip opens
 */
export const DEFAULT_DELAY_DURATION = 0;

/**
 * Default distance in pixels from the tooltip trigger
 */
export const DEFAULT_SIDE_OFFSET = 0;

/**
 * Default distance in pixels from the boundary edges where collision detection occurs
 */
export const DEFAULT_COLLISION_PADDING = 16;
